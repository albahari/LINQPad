<Query Kind="Statements" />

/* The format for a .linq file is straightforward. It consists of an XML header followed by
a blank line and then the source code.

The XML header contains the script's properties (language, connection, configuration, etc). You can
safely edit the file, as long as you keep the XML correctly formed.
	
*/