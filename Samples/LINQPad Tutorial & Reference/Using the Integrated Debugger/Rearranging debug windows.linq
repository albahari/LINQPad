<Query Kind="Statements" />

// You can drag the debug window tabs (Threads, Local, Watch, Call Stack, Results) between the windows 
// on the left and right (or just double-click them). After closing the query, LINQPad forgets any 
// customizations, so you don't have to reset the window layout.

for (int i = 0; i < 10; i++)
{
	int square = i * i;
	square.Dump();
}

// You can switch to a vertical layout with Ctrl+F8 or Query menu | Arrange Results Vertically
