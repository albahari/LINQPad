<Query Kind="Expression" />

/* In most cases, LINQPad automatically shows you the SQL translation for LINQ database queries.

Click the 'SQL' button above the results after running the query. After clicking it, another button
on the right will appear with options to open the SQL in a new query, or export to SSMS.

*/