// LINQPad Statements

using System.Net;

/* LINQPad can also convert SQL to LINQ using a typed DbContext from your own project.

This ensures that the LINQ query uses your own mappings, rather than the mappings
generated automatically by LINQPad.

To proceed:

1. Click 'Add connection' in the Schema Explorer.
   Choose "Typed Data Context from your own assembly", EF Core.

2. Browse to the assembly containing your typed EF Core DbContext
   (this will be in the project's bin/debug folder).

3. Choose the desired DbContext class.

4. Click 'Test' and 'OK' if the connection succeeds.
   Otherwise, click the 'Help me decide' link for help with configuration.
   
5. Right-click the connection and choose "New SQL to LINQ conversion".

*/
