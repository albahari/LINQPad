// LINQPad Statements

using System.Net;

/* The coding agent can access the script's connection schema if you choose.

For example, press Ctrl+I/Command-I and enable "Include Connection Schema".

Then ask it this:

	Show me the biggest one-hit wonders

Or:

	Export the biggest one-hit wonders to Excel 

TIP: This works efficiently with big schemas, too. LINQPad works with the model to figure
     out which portions of the schema it needs, and sends it only those portions.
	 
TIP: The model knows whether the connection is LINQ-to-SQL or EF Core, and whether
     the database is SQL Server, PostgreSQL, SQLite, MySQL or Oracle.  */
