// LINQPad Statements

/* In the Script Properties dialog (F4), there's a button titled 'Export all DLLs to folder'.

This creates a temporary folder and copies all referenced assemblies to that folder. This is handy
when when you have NuGet references, as it includes all the assemblies referenced by the NuGet packages
and their dependencies.

*/