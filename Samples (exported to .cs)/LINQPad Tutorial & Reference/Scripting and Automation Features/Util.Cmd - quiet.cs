// LINQPad Statements

// You can tell LINQPad not to automatically dump the output as follows:

IEnumerable<string> output = Util.Cmd (@"dir", "/od", true);

// You can still access the output by dumping output:
// output.Dump();
